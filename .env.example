# 前端 API 基地址
# 方案：已改用 Cloudflare Pages Functions，API 与前端同域，
# 因此【无需设置，留空即可】，前端会自动走同域 /api/* 路径（无跨域问题）。
#
# 若你确实要把 API 指向另一个域（如独立 Worker），再填到这里，例如：
# VITE_API_BASE=https://ancient-map-worker.xxx.workers.dev
VITE_API_BASE=
