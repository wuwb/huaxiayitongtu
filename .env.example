# 百度地图 AK（Cloudflare Pages 环境变量名必须是 VITE_ 前缀）
# 也可不设置，前端已内置默认 AK。
VITE_BAIDU_AK=

# 前端 API 基地址
# 已改用 Cloudflare Pages Functions，API 与前端同域，【无需设置，留空即可】。
# 若确实要指向其它域（如独立 Worker），再填，例如：
# VITE_API_BASE=https://ancient-map-worker.xxx.workers.dev
VITE_API_BASE=
